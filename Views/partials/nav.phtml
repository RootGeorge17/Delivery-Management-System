<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="dimensions container-fluid px-4 py-6">
        <a class="navbar-brand text-white" href="/">ParcelPal</a>
        <button class="navbar-toggler navbar-dark" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false"
                aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between" id="navbarNavDropdown">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link text-white" aria-current="page" href="/">Dashboard</a>
                </li>
            </ul>

            <div class="-flex flex-sm-grow-1 me-2 pt-2 mb-2" style="max-width: 800px;">
                <?php require_once('Views/partials/searchForm.phtml') ?>
            </div>

            <div class="d-flex align-items-center">
                <?php if(isset($_SESSION['user']['username'])) : ?>
                    <a class="text-white" href="/signout">
                        <button type="button" class="btn btn-primary px-3 me-2">
                            Sign out
                        </button>
                    </a>
                <?php else : ?>
                    <a class="text-white" href="/login">
                        <button type="button" class="btn btn-primary px-3 me-2">
                            Log in
                        </button>
                    </a>
                <?php endif; ?>
            </div>
        </div>
    </div>
</nav>