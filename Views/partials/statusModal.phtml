<!-- Status Dynamic Modal -->
<div class="modal fade" id="statusModal" tabindex="-1" aria-labelledby="pendingModalLabel" aria-hidden="true">
    <!-- Modal content for Pending -->
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <!-- Your modal content for "Pending" -->
            <div class="modal-header">
                <h5 class="modal-title" id="statusModalLabel">Change Status</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <?php
                // Process URL parameters to change status or perform any necessary actions
                $deliveryId = $_GET['delivery-id'] ?? '';
                $status = $_GET['status'] ?? '';

                // Display the delivery ID and status within the modal
                echo 'Delivery ID: ' . $deliveryId . '<br>';
                echo 'Status: ' . $status . '<br>';
                ?>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                <button type="button" class="btn btn-primary">Confirm Change</button>
            </div>
        </div>
    </div>
</div>
